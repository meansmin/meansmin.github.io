# 노션 관리자 페이지 사용법

## 어디에 있나

노션 → `C&C F. 홈페이지 관리` 페이지 → `앱 & 게임` 데이터베이스

## 속성 설명

| 속성 | 쓰는 법 |
|---|---|
| 이름 | 한국어 앱 이름. **필수** |
| Name (EN) | 영어 앱 이름. 비우면 한국어 이름이 영어 페이지에도 나온다 |
| 슬러그 | 주소가 된다. 영어 소문자·숫자·하이픈만. **중복되면 그 항목은 사이트에서 빠진다** |
| 종류 | 게임 / 앱 |
| 상태 | 출시 / 개발 중 / 기획 중 / 보류 |
| **공개** | **이 체크박스를 켜야 사이트에 나온다** |
| 플랫폼 | Android 등 |
| 한 줄 소개 | 목록과 상세 상단에 나오는 짧은 문장 |
| Tagline (EN) | 영어 한 줄 소개 |
| 아이콘 | 정사각형 이미지 1장 (512px 권장) |
| 스크린샷 | 여러 장. 세로 화면으로 통일하면 보기 좋다 |
| 스토어 링크 | Play 스토어 주소 |
| 방침 링크 | 개인정보처리방침 주소 |
| 출시일 | 목록에 연·월로 표시된다 |
| 태그 | 목록 카드에 최대 3개까지 보인다 |
| 정렬 | 숫자가 작을수록 위에 온다 |

## 본문 쓰는 법

페이지를 열고 본문에 상세 소개를 쓴다. 쓸 수 있는 것은 다음과 같다.

- 문단
- `## 소제목`, `### 작은 소제목`
- 글머리 기호 목록, 번호 목록
- **굵게**, *기울임*, `코드`, [링크](https://example.com)
- 인용구, 구분선

영어 본문은 같은 페이지 안에서 `## EN` 이라는 제목을 하나 넣고 그 아래에 쓴다.
`## EN` 위쪽은 한국어, 아래쪽은 영어로 나뉜다.

## 반영 시점

노션에서 고친 내용은 **빌드가 돌아야** 사이트에 반영된다.

- 즉시 반영: GitHub → Actions → `사이트 빌드 및 배포` → Run workflow
- 자동 반영: 매일 오전 6시

## 주의

- 이미지는 노션에 올리면 빌드할 때 내려받아 저장한다. 노션의 원본 주소는 1시간이면 만료되므로 **사이트가 노션을 직접 참조하지 않는다**
- 슬러그를 바꾸면 기존 주소가 깨진다. 이미 공유한 링크가 있으면 바꾸지 않는 편이 좋다
